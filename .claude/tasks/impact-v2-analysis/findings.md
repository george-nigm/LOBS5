# Находки

- V2 папки находятся в context_500_buy/ и context_500_sell/ (а не отдельно)
- Старые V1 папки (без _v) уже удалены — только V2 остались
- Формат имени: i{i}_c{c}_mb{mb}_v{V}_cntxt{pct}%
- aggressive_indices.csv одинаковы для всех V при фиксированных (i,mb) — как и ожидалось
- sample_day_map.csv: 2048 строк, колонки: sample_id, day, stock, highest_price, lowest_price, execution_sum
