# CST Model Aggressive Scenario — SMALL TEST CONFIG
# Run inside Docker container

# Data (paths inside Docker container)
data_dir: "/home/myuser/data/processed_data/GOOG/2023_Jan"
stock: "GOOG"
tick_size: 100

# CST Model
params_file: "/home/myuser/scratch/cst_params_goog.pkl"
num_ticks: 500
n_levels: 10

# Sequence
n_cond_msgs: 500
n_gen_msgs: 10
n_eval_msgs_dataset: 500

# Insertions — small test
num_insertions: 1
num_coolings: 3
event_type: 4
direction: 0
order_volume: 75

# Sampling — just one batch
n_samples: 16
batch_size: 16
rng_seed: 42
test_split: 0

# Output
save_dir: "/home/myuser/data/evalsequences/cst_scenario_test"
